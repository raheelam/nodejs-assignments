<%- include('includes/top.ejs') %>

</head>
    <body>
        <main>
            <h1>List of Users</h1>
            <% if(users.length > 0){ %>
                <ul>
                    <% for(let user of users){ %>
                        <li><%= user %></li>
                    <% } %>
                </ul>
            <% } else{ %>
                <p>No users yet!</p>
            <% } %>
        </main>
        

<%- include('includes/bottom.ejs') %>